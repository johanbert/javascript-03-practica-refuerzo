// if (numero > 5000) {
//     return 'el numero es grande'
// } else {
//     return 'el numero es pequeño'
// }
const validarNumero = (numero) => {
    // numero = numero || 20000
    return (numero > 5000) ? 'el numero es grande' : 'el numero es pequeño'
}